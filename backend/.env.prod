# --- PRODUCTION ENVIRONMENT VARIABLES FOR DJANGO ---

# In production, DEBUG MUST be False.
DJANGO_DEBUG=False

# In production, you MUST set a strong, unique secret key.
# Generate one with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=your_strong_production_secret_key_here

# In production, set this to your actual domain(s).
# Example: DJANGO_ALLOWED_HOSTS=mycraft.com,api.mycraft.com
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# In production, set this to your frontend's actual domain.
# Example: DJANGO_CORS_ALLOWED_ORIGINS=https://mycraft.com
DJANGO_CORS_ALLOWED_ORIGINS=http://localhost

# --- Production Database (Example for PostgreSQL) ---
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=mycraft_prod
# DB_USER=mycraft_user
# DB_PASSWORD=your_strong_db_password
# DB_HOST=db # This would be the name of your database service in docker-compose
# DB_PORT=5432
