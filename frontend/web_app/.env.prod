# --- PRODUCTION ENVIRONMENT VARIABLES FOR VUE ---

# In production, the API might be served from the same domain or a subdomain.
# This should point to your production backend URL.
# Example: VITE_API_URL=https://api.mycraft.com/api
VITE_API_URL=/api
